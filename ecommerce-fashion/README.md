
  # Convert Image to Editable

  This is a code bundle for Convert Image to Editable. The original project is available at https://www.figma.com/design/zBZzP9aICJzqsvVuxyD6ms/Convert-Image-to-Editable.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  .
